/*
일단 정상 실행 하기 위해 서칭해서 수정한 임시 build.gradle 입니다.
저도 밑 내용들이 어떤 의미인지 잘 모르며 PR 의도로 작성 한 것이 아닙니다.
이렇게 작성하니 실행은 가능 해졌기 때문에 참고만 해주시면 감사하겠습니다.
*/

plugins {
    id 'java'
    id 'application'
    id 'com.gradleup.shadow' version '9.0.0-beta3'
}

dependencies {
    implementation project(':common')
    testImplementation project(':common')
}

application {
    mainClass = 'dev.qf.client.Main'
}

shadowJar {
    manifest {
        attributes 'Main-Class': 'dev.qf.client.Main'
    }
    exclude 'META-INF/*.SF', 'META-INF/*.DSA', 'META-INF/*.RSA'
}
